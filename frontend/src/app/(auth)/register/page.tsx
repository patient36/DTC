import RegistrationForm from "@/components/auth/RegistrationFom";

export const metadata = {
    title: 'Sign Up'
}

const RegistrationPage = () => {
    return (
        <RegistrationForm />
    );
}

export default RegistrationPage;