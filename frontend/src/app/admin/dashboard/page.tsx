import AdminDashboard from "@/components/admin.dashboard"
export const metadata = {
    title: "Admin  •  Dashboard"
}
const page = () => {
    return (
        <AdminDashboard />
    )
}

export default page
