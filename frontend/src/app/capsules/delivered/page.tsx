export const metadata = {
  title: 'Delivered Capsules',
};

import DeliveredCapsules from "./caspules";

const Capsules = () => {
    return (
        <DeliveredCapsules />
    );
}

export default Capsules;