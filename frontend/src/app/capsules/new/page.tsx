import NewCapsuleForm from "./createCapsule";

export const metadata = {
    title: 'Capsules - Create New Capsule',
};
const Page = () => {
    return (
        <NewCapsuleForm />
    );
}

export default Page;
